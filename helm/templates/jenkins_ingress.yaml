apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jenkins
  namespace: {{ .Values.namespaces.jenkins_namespace }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  labels:
    app: deploy-jenkins
spec:
  rules:
  - host: {{ .Values.namespaces.jenkins_namespace }}.local
    http:
      paths:
        - path: /([ ]*)(.*)
          backend:
            serviceName: jenkins
            servicePort: 80
