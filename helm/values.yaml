# Default values for matts jenkins.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

metadata:
  appVersion: "1.0"
  nexus_namespace: nexus

jenkins:
  namespace: jenkins
  volumes:
    #path: /var/lib/docker/volumes/diab_jenkins/jenkins_home
    #path: /var/lib/docker/volumes/diab/jenkins/jenkins_home
    path: /var/lib/docker/volumes/jenkins4kubes/jenkins_home
    type: DirectoryOrCreate